import numpy as np

# Array creation helpers
n = 100
a = np.zeros((1, n))
b = np.ones((n, 1))
c = np.linspace(0, 2*np.pi, n)
d = np.logspace(-2, 2, 50)
e = np.random.rand(4, 4)
f = np.random.randn(1, 1000)
g = np.eye(5)
h = n*np.ones(3)
