# Telemetry digest (MATLABtoPython) — pending first run

_This file is overwritten weekly by `.github/workflows/telemetry-digest.yml`
(Mondays 07:00 UTC) from `usage_events` where `site = 'matlab'`. Run manually
with `npm run telemetry:digest` once the SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY
secrets/env are set (point them at the shared vbatopython Supabase project)._

No events yet — the consent toggle ships off-by-default for signed-in users and
on for anonymous visitors. Volume, not code, is the remaining gap.
